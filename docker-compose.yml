services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ai_world
    ports:
      - "5432:5432"
    volumes:
      - ai_world_pgdata:/var/lib/postgresql/data

volumes:
  ai_world_pgdata:
