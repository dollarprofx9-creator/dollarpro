name: Biweekly Telegram Consistency Message

on:
  schedule:
    # Runs at 04:00 UTC every Monday to Friday
    - cron: "0 4 * * 1-5"
  workflow_dispatch:

jobs:
  send-message:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Check if this is a posting week
        id: week
        run: |
          WEEK=$(date +%V)
          echo "Current week: $WEEK"
          if [ $((10#$WEEK % 2)) -eq 0 ]; then
            echo "post=true" >> $GITHUB_OUTPUT
          else
            echo "post=false" >> $GITHUB_OUTPUT
          fi

      - name: Run script
        if: github.event_name == 'workflow_dispatch' || steps.week.outputs.post == 'true'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python en.py
